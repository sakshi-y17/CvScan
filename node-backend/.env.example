# Port configuration (default 5001 to prevent conflicts with Flask on 5000)
PORT=5001

# MySQL Database connection configuration
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=cvscan_db
